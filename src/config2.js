// const mongoose = require('mongoose');

// // Product Schema
// const productSchema = new mongoose.Schema({
//     name: { type: String, required: true },
//     price: { type: Number, required: true }, // Store price in cents if you're planning to use it for payments
//     image: { type: String, required: true },
//     description: { type: String, required: false }, // Optional description field
//     stock: { type: Number, default: 0 }, // Optional stock control
// });

// // Create Product model
// const Product = mongoose.model('Product', productSchema);

// // Export Product model
// module.exports = { Product };
